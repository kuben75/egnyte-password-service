version: '3.8'

services:
  password-evaluator:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: password-service
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
    restart: unless-stopped